# Graphiti Configuration
# Copy this file to .env and update with your values

# Group ID for Graphiti memory scope
VITE_GROUP_ID=your-group-id

# Graphiti Server
# The main Graphiti server (provides both HTTP API and WebSocket)
# Format: http://host:port or https://host:port
VITE_GRAPHITI_SERVER=http://172.16.0.14:3060

# LlamaCPP Server
# Local LLM inference server
VITE_LLAMACPP_URL=http://localhost:9004

# Chat Backend Service (graphiti-chat)
# The backend service handles ToolLoopAgent + CodeMode with Graphiti memory
VITE_CHAT_API_URL=http://localhost:3001

# Xerro Service (Obsidian documents)
# The xerro-service provides access to Obsidian vault documents
VITE_XERRO_SERVICE_URL=http://localhost:9205

# Xerro API URL (remote/production xerro-service endpoint)
# Used for authentication and production deployments
# Falls back to VITE_XERRO_SERVICE_URL if not set
VITE_XERRO_API_URL=https://xerro-mb-api.appkit.net

# Document Navigation
# Comma-separated list of folder names to hide from the documents navigation
VITE_HIDDEN_FOLDERS=.git,Assets,Images,thumbnails,Uncategorized

# System Control Panel

# Llamacpp Admin
# In development: Uses Vite proxy at /llamacpp-admin (set target in vite.config.ts)
# In production: Direct URL to llamacpp admin server
# Example: http://172.16.0.75:9200 or http://localhost:8004
VITE_LLAMACPP_ADMIN_URL=http://localhost:8004

# Bearer token for llamacpp admin authentication
# Get this from llamacpp-cli admin service logs or configuration
# Run: llamacpp admin start --verbose to see the API key
VITE_LLAMACPP_ADMIN_TOKEN=your-bearer-token-here
